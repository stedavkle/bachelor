\ProvidesPackage{wsi-ra}

% Fallunterscheidung danach, ob PDFLaTeX oder LaTeX benutzt wird definieren:
\usepackage{ifpdf}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Wichtige Pakete               %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{ngerman}		% Unsterstützung der aktuellen deutschen Rechtschreibung
%\usepackage{babel}		    % Unterstützung mehrsprachiger Dokumente, Spache als Option angeben.
\usepackage[utf8]{inputenc}	% Erlaubt direkte eingabe aller deutschen Schriftzeichen

% Ersetzung der Standard-Schriftarten
\usepackage{courier}		% Schreibmaschinen-Schrift
\usepackage{mathptmx}		% Standardschriftart für den Fließtext
\usepackage[scaled=.9]{helvet}	% Standardschriftart für serifenlosen Text
\usepackage{eurosym}		% Eurosymbol als \euro

% Stil
\usepackage{calc}     % Satzspiegelberechnung
\usepackage[paper=a4paper,
            landscape=false,
            hmargin=2.54cm,
            vmargin=2.54cm,
            centering]{geometry}
% Nutzt die A4-Seiten besser aus und setzt den Satzspiegel neu.
\usepackage{fancyhdr}	% Gestaltung der Kopf- und Fußzeilen
\usepackage{bibgerm}	% Deutsche Spracheinstellung für den Zitierstil
\usepackage{url}		  % Schönere Darstellung von Pfadangaben sowie Links ins Web
				              % bzw. zum E-Mail-Klienten

% PDFLaTeX- bzw. LaTeX-spezifische Einstellungen

\ifpdf
  \usepackage[pdftex]{graphicx}
  \usepackage[pdfpagemode={UseOutlines},
              pdftitle={Lokalisierung von Messspitzen im
Rasterelektronenmikroskop durch
Bildsegmentierung mittels Deep Learning},
              pdfauthor={David Stefan Kleindiek},
              pdfsubject={Bachelorarbeit Informatik},
              pdfkeywords={},
              pdfview={FitBH},
              plainpages={false},
              pdftex,
              pdfdisplaydoctitle=true,
              colorlinks={false},
              bookmarks,
              bookmarksopen,
              bookmarksnumbered,
              pdfhighlight={/P},
              urlcolor={blue}]{hyperref} % Ermöglicht Links im Dokument.
  % Für Tabellen oder Abbildungen, die eine Seite im Querformat beanspruchen
  \usepackage{pdflscape}	% Funktioniert nur mit PDF-LaTeX!
   \pdfcompresslevel=9
\else
  \usepackage{graphicx}	% Einbinden von Grafik-Dateien
  \usepackage{epsfig}		% Einbinden von EPS-Graphiken
  \usepackage{psfig}		% Einbinden von PS-Graphiken
  \usepackage[plainpages={false}]{hyperref} % Ermöglicht Links im Dokument.
  % Für Tabellen oder Abbildungen, die eine Seite im Querformat beanspruchen
  \usepackage{lscape}		% Funktioniert nur mit dem herkömmlichen LaTeX!
\fi

% Weitere notwendige Pakete für Grafiken und Farbe
\usepackage{subfigure}% Unterabildungen in figure-Umgebungen
\usepackage{color}		% Farbiger Text ermöglicht


% Mathematikpakete
\usepackage{nicefrac}		% Schönere Darstellung von Brüchen im Text.
\usepackage{amsmath}		%
\usepackage{amsthm}		%
\usepackage{amscd}		%
\usepackage{amsfonts}		% Mathematische Schriftarten
\usepackage{amssymb}		% Mathematik-Symbole
\usepackage{wasysym}		% Gibt eine Reihe zusätzlicher Symbole, z. B. Widerspruchsblitz.

% Chemiepaket
\usepackage[version=3]{mhchem}
				% Erlaubt chemische Formeln

% Für Tabellen
\usepackage{longtable}  % Unterstützt lange Tabellen, die über mehrere Seiten
                        % gehen.
\usepackage{multirow} 	% Stellt den Befehl für Zellen über mehrere Zeilen zur
                        % Verfügung.
\usepackage{array}      % Enthält zusätzliche Befehle für komplexe Tabellen
\usepackage{booktabs}		% Produziert besonders schöne Tabellen.
%\usepackage{dcolumn}		% Ermöglicht nach Dezimalstellen formatierte Tabelleneinträge
\usepackage{hhline}     % Erweiterte Möglichkeiten für Linien in Tabellen

% Algorithmuspakete
\usepackage[Algorithmus]{algorithm}
                	        % Ermöglicht Einbindung von Algorithmen
\usepackage{algorithmic} % Algorithmen im Pseudo-Code,
				% die dann auch ins Algorithmenverzeichnis aufgenommen werden
\usepackage{listings}		% Quellcode einbinden. Mit \lstset{language=C++} o. Ä. wie
				% C, C++, Java, VHDL, Ada, Fortran, HTML, Mathematica, Pascal, Perl,
				% Python... Sprache wählen und mit \lstinputlisting{meine-datei.cpp}
				% einfach einbinden.

% Für den Anhang
\usepackage{abbrev}	% Bietet ein Abkürzungsverzeichnis für Juristen
% Index und Glossar
\usepackage{makeidx}		% Ermöglicht Erstellung eines Indexes
\usepackage[toc=true, number=none, style=altlist]{glossary}
				                % Ermöglicht Erstellung eines Glossars


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Einstellungen                 %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Setzt alle Überschriften und Beschreibungen auf die fette
% Fließtextschriftart.
\setkomafont{descriptionlabel}{\bfseries}
\setkomafont{sectioning}{\bfseries}

\pagestyle{headings}		% Schaltet die Kopfzeilen ein.

% Einstellungen für das Listings-Paket
\lstset{basicstyle=\small\ttfamily, stringstyle=\ttfamily, keywordstyle=\bf\ttfamily}

% Einstellungen für das Algorithmus-Paket:
\renewcommand{\algorithmicrequire}{\textbf{Erfordert:}}
\renewcommand{\algorithmicensure}{\textbf{Liefert:}}
\renewcommand{\listalgorithmname}{Algorithmenverzeichnis}
% Einstellungen für das Listings-Paket:
\renewcommand{\lstlistingname}{Quellcodeauszug}

% weitere Definitionen für den Glossar, um die deutsche Sprache einzustellen:
\renewcommand{\glossaryname}{Glossar}
\renewcommand{\descriptionname}{Beschreibung}

% Definitionen, Theoreme, Sätze und Beweise
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}
\newtheorem{corollar}[theorem]{Corollar}
\newtheorem{lemmma}[theorem]{Lemma}
\newtheorem{satz}{Satz}
\theoremstyle{remark}
\newtheorem*{bem}{Bemerkung}
\newtheorem{lem}{Lemma}

\renewcommand{\proofname}{\textbf{Beweis}}
\renewcommand{\qedsymbol}{$\blacksquare$}
